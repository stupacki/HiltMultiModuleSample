apply from: "$rootProject.rootDir.absolutePath/androidLibModule.gradle"

android {
    namespace 'com.stupacki.hilt.app.domain'
}

dependencies {

    implementation project(':common:core')
    implementation project(':common:data')
}
